import modbus_tk
import bacpypes
import serial
import kivy

'''
Objective:

Create a Master/Slave example in the terminal that communicates using Modbus & BACnet, ideally at the same time.
* Potentially need a Ethernet port for this
* Raspberry Pi will act as slave, computer can suffice as both sides for now
* Start with Modbus, add BACnet
'''


def main():
    print("Hello World")


if __name__ == "__main__":
    main()
